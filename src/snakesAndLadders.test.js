const snakesAndLadders = require('./snakesAndLadders');

describe('snakesAndLadders', () => {
  it('snakesAndLadders should return snakesAndLadders', () => {
    expect(hello()).toBe('snakesAndLadders');
  });
});
